# Content Editor Agent (Java Port)

This agent is a JavaScript port of the Java Content Editor Agent.

```bash
export GEMINI_API_KEY=<your_api_key>
npm run agents:content-editor-agent-js
```

The agent will start on `http://localhost:10003`.