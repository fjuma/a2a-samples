# Content Editor Agent

This agent edits content provided to it.

```bash
export GEMINI_API_KEY=<your_api_key>
npm run agents:content-editor-agent
```

The agent will start on `http://localhost:41243`.